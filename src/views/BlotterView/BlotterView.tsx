import { PlaceholderView } from "@/views/PlaceholderView";

export function BlotterView() {
  return (
    <PlaceholderView
      title="Blotter Service"
      description="Primary operational view: books with top-level PnL, filterable trades, alpha/beta, and live valuations from the SSE stream."
    />
  );
}
