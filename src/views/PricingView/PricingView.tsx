import { PlaceholderView } from "@/views/PlaceholderView";

export function PricingView() {
  return (
    <PlaceholderView
      title="Pricing Service"
      description="Valuation stream with fair value, realized/unrealized PnL, and book-level alpha/beta metrics."
    />
  );
}
