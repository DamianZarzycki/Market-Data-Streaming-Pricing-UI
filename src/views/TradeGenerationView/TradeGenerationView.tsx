import { PlaceholderView } from "@/views/PlaceholderView";

export function TradeGenerationView() {
  return (
    <PlaceholderView
      title="Trade Generation Service"
      description="Generator status and configuration: frequency, allowed asset classes, trade-out probability, and recent generated events."
    />
  );
}
