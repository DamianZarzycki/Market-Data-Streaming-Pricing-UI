import { PlaceholderView } from "@/views/PlaceholderView";

export function MarketDataView() {
  return (
    <PlaceholderView
      title="Market Data Service"
      description="Real-time ticks table, symbol/data-class filters, LIVE/STALE markers, and a mini price history driven by the market-data SSE stream."
    />
  );
}
