import { PlaceholderView } from "@/views/PlaceholderView";

export function SystemOverview() {
  return (
    <PlaceholderView
      title="System Overview"
      description="Service status cards, aggregate realized/unrealized PnL, active trades, book count, and last market/valuation timestamps will live here."
    />
  );
}
